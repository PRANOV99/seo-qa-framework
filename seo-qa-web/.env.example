# URL of the deployed backend API
# For local development, leave blank — the Vite proxy handles /api/* requests
# For production (Vercel), set to your Render backend URL
VITE_API_URL=https://your-render-api.onrender.com
